import styled from 'styled-components'
import {Search} from '../Styled'

export const StyledCardActor =styled(Search)`
    .deathday {
        margin: 0;
        margin-top: 15px;
        font-weight: bold;
    }    
`